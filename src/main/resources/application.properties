spring.application.name=demo1
wx.appid=${WX_APPID}
wx.secret=${WX_SECRET}
wechat.appid=${WX_APPID}
wechat.secret=${WX_SECRET}
auth.token.secret=${AUTH_TOKEN_SECRET:change-me}
auth.token.ttlSeconds=${AUTH_TOKEN_TTL_SECONDS:2592000}
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?sslmode=require&preferQueryMode=simple}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.frrmyvqqmlffxurhjejf}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
spring.jpa.hibernate.ddl-auto=none
spring.flyway.enabled=true
# 禁用Flyway的校验功能，解决校验和不匹配问题
spring.flyway.validate-on-migrate=false
# 对 PgBouncer 事务池禁用服务端预编译语句，避免 "prepared statement S_1 already exists"
spring.datasource.hikari.data-source-properties.preferQueryMode=simple
# 静态资源映射
spring.mvc.static-path-pattern=/uploads/**
spring.web.resources.static-locations=file:./uploads/
# 文件上传配置
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
